name: Deploy production
run-name: Deploy full reviewed candidate by @${{ github.actor }}

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy all components
    uses: ./.github/workflows/_deploy-production-scope.yml
    with:
      release-scope: all
