# Production checkout of github.com/soloser/getreplay-migrations.
MIGRATIONS_DIR=/home/solo/fun-migrations/migrations
MIGRATIONS_REMOTE=origin
MIGRATIONS_BRANCH=main

# Keep each complete URL quoted. URL-encode reserved characters in credentials.
MYSQL_DATABASE_URL="mysql://login:password@tcp(host:3306)/db"
CLICKHOUSE_DATABASE_URL="clickhouse://host:9000?username=username&password=password&database=db"
